# ADK agents are optional - we use tools directly for now
# from google.adk.agents import LlmAgent
from config.settings import settings

# We use tools directly in the API routes instead of agent orchestration
